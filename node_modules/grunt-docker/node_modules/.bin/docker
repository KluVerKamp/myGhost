#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../docker/docker" "$@"
  ret=$?
else 
  node  "$basedir/../docker/docker" "$@"
  ret=$?
fi
exit $ret
